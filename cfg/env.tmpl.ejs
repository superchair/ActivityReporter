'use strict';
(function() {
    var env = angular.module('{%- moduleName %}'{% if (deps) { %}, {%= JSON.stringify(deps) %}{% } %});
    {% constants.forEach(function (constant) { %}
    env.constant('env:{%- constant.name %}', {%= constant.value %});{% }); %}
    {% values.forEach(function (value) { %}
    env.value('env:{%- value.name %}', {%= value.value %});{% }) %}
})();
